# System module API paths

# Global system configuration (sys_ini singleton, all sections)
config:
  $ref: './system-config.yaml'

# Sites configuration section
sites-config:
  $ref: './sites-config.yaml'

# Mail configuration section
mail-config:
  $ref: './mail-config.yaml'

# DNS configuration section
dns-config:
  $ref: './dns-config.yaml'

# Domains configuration section
domains-config:
  $ref: './domains-config.yaml'

# Miscellaneous configuration section
misc-config:
  $ref: './misc-config.yaml'

# DNS Certification Authorities (CAA policy records)
dns-cas:
  $ref: './dns-cas.yaml'

# Directive snippets
directive-snippets:
  $ref: './directive-snippets.yaml'

# Service resynchronization
resync:
  $ref: './resync.yaml'
