# Monitor module API paths (all read-only)

# Data logs (sys_datalog journal)
data-logs:
  $ref: './data-logs.yaml'

# Server status (computed from monitor_data)
server-status:
  $ref: './server-status.yaml'

# System logs (sys_log)
system-logs:
  $ref: './system-logs.yaml'
