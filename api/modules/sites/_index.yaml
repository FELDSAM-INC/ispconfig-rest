# Main index file for Sites module paths
# This file combines all the individual path files

# Web domains (vhosts) — parent resource of everything below
web-domains:
  $ref: './web-domains.yaml'

# Subdomains and alias domains
web-child-domains:
  $ref: './web-child-domains.yaml'

# FTP users
ftp-users:
  $ref: './ftp-users.yaml'

# Shell users
shell-users:
  $ref: './shell-users.yaml'

# Databases
databases:
  $ref: './databases.yaml'

# Database users
database-users:
  $ref: './database-users.yaml'

# Cron jobs
cron-jobs:
  $ref: './cron-jobs.yaml'

# Protected web folders
web-folders:
  $ref: './web-folders.yaml'

# Web folder users
web-folder-users:
  $ref: './web-folder-users.yaml'

# WebDAV users
webdav-users:
  $ref: './webdav-users.yaml'
