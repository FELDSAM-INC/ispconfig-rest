# DNS module API paths

# DNS SOA (Start of Authority) zone paths
soa:
  $ref: './soa.yaml'

# DNS Resource Record paths
records:
  $ref: './records.yaml'

# DNS Slave (secondary) Zone paths
slaves:
  $ref: './slave.yaml'

# DNS Template paths
templates:
  $ref: './template.yaml'
