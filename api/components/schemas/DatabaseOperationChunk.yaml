type: object
required: [dump_base64]
additionalProperties: false
properties:
  dump_base64: {type: string, format: byte, minLength: 4, maxLength: 11184812, description: 'Strict base64, up to the negotiated chunk_size (maximum 8 MiB). All chunks except the last must be exactly chunk_size bytes.'}
