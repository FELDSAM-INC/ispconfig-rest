# This file serves as an index for all response definitions
BadRequest:
  $ref: './BadRequest.yaml'

Unauthorized:
  $ref: './Unauthorized.yaml'

Forbidden:
  $ref: './Forbidden.yaml'

NotFound:
  $ref: './NotFound.yaml'

Conflict:
  $ref: './Conflict.yaml'

InternalServerError:
  $ref: './InternalServerError.yaml'

UnprocessableEntity:
  $ref: './UnprocessableEntity.yaml'