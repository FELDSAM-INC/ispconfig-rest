<?php

// DNS module routes (required by routes/api.php inside the api.key group).
// TODO: register dns module resource routes here — follow the pattern in
// routes/api/mail.php (explicit Route:: calls, specific before general).
